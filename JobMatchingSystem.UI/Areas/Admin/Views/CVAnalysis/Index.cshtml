@{
    ViewData["Title"] = "Phân tích CV bằng AI";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">
                        <i class="fas fa-robot mr-2"></i>
                        Phân tích CV bằng AI
                    </h3>
                </div>
                
                <div class="card-body">
                    @if (TempData["Error"] != null)
                    {
                        <div class="alert alert-danger alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert">&times;</button>
                            <strong>Lỗi!</strong> @TempData["Error"]
                        </div>
                    }

                    <div class="row">
                        <!-- Single File Analysis -->
                        <div class="col-md-6">
                            <div class="card card-primary">
                                <div class="card-header">
                                    <h5 class="card-title">Phân tích một file CV</h5>
                                </div>
                                <div class="card-body">
                                    <form asp-action="AnalyzePDF" method="post" enctype="multipart/form-data">
                                        <div class="form-group">
                                            <label for="pdfFile">Chọn file CV (PDF):</label>
                                            <div class="input-group">
                                                <div class="custom-file">
                                                    <input type="file" class="custom-file-input" id="pdfFile" name="pdfFile" accept=".pdf" required>
                                                    <label class="custom-file-label" for="pdfFile">Chọn file...</label>
                                                </div>
                                            </div>
                                            <small class="form-text text-muted">Chấp nhận file PDF, tối đa 5MB</small>
                                        </div>
                                        <button type="submit" class="btn btn-primary">
                                            <i class="fas fa-search mr-1"></i> Phân tích CV
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>

                        <!-- Batch Analysis -->
                        <div class="col-md-6">
                            <div class="card card-success">
                                <div class="card-header">
                                    <h5 class="card-title">Phân tích nhiều file CV</h5>
                                </div>
                                <div class="card-body">
                                    <form asp-action="BatchAnalyze" method="post" enctype="multipart/form-data">
                                        <div class="form-group">
                                            <label for="pdfFiles">Chọn nhiều file CV (PDF):</label>
                                            <div class="input-group">
                                                <div class="custom-file">
                                                    <input type="file" class="custom-file-input" id="pdfFiles" name="pdfFiles" accept=".pdf" multiple required>
                                                    <label class="custom-file-label" for="pdfFiles">Chọn file...</label>
                                                </div>
                                            </div>
                                            <small class="form-text text-muted">Có thể chọn nhiều file PDF cùng lúc</small>
                                        </div>
                                        <button type="submit" class="btn btn-success">
                                            <i class="fas fa-layer-group mr-1"></i> Phân tích hàng loạt
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Sample Excel Download -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <div class="card card-info">
                                <div class="card-header">
                                    <h5 class="card-title">Mẫu kết quả xuất Excel</h5>
                                </div>
                                <div class="card-body">
                                    <p>Tải xuống file Excel mẫu để xem định dạng kết quả phân tích CV:</p>
                                    <a href="@Url.Action("DownloadSampleExcel")" class="btn btn-info">
                                        <i class="fas fa-download mr-1"></i> Tải Excel mẫu
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Instructions -->
                    <div class="row mt-4">
                        <div class="col-12">
                            <div class="card card-warning">
                                <div class="card-header">
                                    <h5 class="card-title">Hướng dẫn sử dụng</h5>
                                </div>
                                <div class="card-body">
                                    <ul>
                                        <li><strong>AI Service:</strong> Cần cài đặt Ollama và model llama3.1 để sử dụng tính năng AI phân tích CV</li>
                                        <li><strong>File PDF:</strong> Chấp nhận file PDF có nội dung text (không phải scan ảnh)</li>
                                        <li><strong>Kết quả:</strong> AI sẽ trích xuất thông tin: tên, năm sinh, học vấn, kỹ năng, kinh nghiệm...</li>
                                        <li><strong>Xuất Excel:</strong> Tất cả kết quả có thể xuất ra file Excel để quản lý</li>
                                        <li><strong>Lưu ý:</strong> Nếu chưa cài AI, hệ thống vẫn có thể trích xuất text từ PDF</li>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Update file input label when files are selected
        $('.custom-file-input').on('change', function() {
            let fileName = '';
            if (this.files && this.files.length === 1) {
                fileName = this.files[0].name;
            } else if (this.files && this.files.length > 1) {
                fileName = this.files.length + ' files selected';
            } else {
                fileName = 'Choose file...';
            }
            $(this).siblings('.custom-file-label').addClass('selected').html(fileName);
        });
    </script>
}